# Editorial v1.1 Implementation Plan

1. Add focused failing tests for v1.1 routing, shared references, accessible
   SVG naming, grid/layout rules, and rounded connector guidance.
2. Add the versioned profile plus shared node-layout, output-contract, and
   acceptance documents; update the inspiration registry and Skill routing.
3. Revise the architecture template and golden example to demonstrate the
   rules and regenerate the user-facing desktop HTML.
4. Run focused tests, render once at desktop width, inspect the screenshot,
   and commit the branch.
